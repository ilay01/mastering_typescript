"use strict";
// undefined
// here we dont know the type of the variable, is ut a number? string? whatsoever?
// Null
// we know the type but not the value, so its null 
// Conditional Expressions
// if is ? und then is : 
// (conditional) ? (true statement) : (false statement);
const value = 10;
const message = value > 10 ?
    "value is larger than 10" : "value is 10 or less"; // can also be chained
console.log(message);
// Optional chaining
